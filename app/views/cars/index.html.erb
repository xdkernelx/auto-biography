<h1>My Cars</h1>
<% if user_signed_in? %>
	<ul>
		<% @cars.each do |car| %>
		<li><%= car.year %> |
		 <%= link_to car.make, car_path(car)%> |
		<%= car.model %> |

		<%= link_to 'edit', edit_car_path(car) %>
		</li>
	<% end %>
	</ul>
	<% else %>
		sign in 
<% end %>

