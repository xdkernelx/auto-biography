<div class="col-sm-3 col-md-6 issue-half">
  <% if current_user %> 
  <h1 class="maps-page" id ="map-left-side">Issue</h1>
    <h3><%=@issue.title%></h3>
    <%= @issue.description %><br>
    <span>Urgency: <%= @issue.urgency %></span><br><br>
    <%= link_to "Edit", edit_car_issue_path(@car, @issue.id), class: "btn btn-back-red" %><br><br>
    <%= link_to 'Back', car_issues_path(@car), class: "btn btn-back-blue" %>
  </div>
<% else %>
  <h3>You must be logged in to view this page</h3>
<% end %>
</div>
<div class="col-sm-3 col-md-6" id="map-right-side">
    <h1 class="maps-page map-right-side"> Repairs for issue: </h1>
      <% if mechanic? %>
      <%= link_to "Add Repair", new_car_issue_repair_path(@car, @issue), method: "get", class: "btn btn-info"  %>
    <% else %>
      <%= link_to "Add Repair", new_car_issue_repair_path(@car, @issue), method: "get", class: "btn btn-info" %>
    <% end %><br>
      <% @repairs.each do |repair| %><br>
      <%= repair.title %><br>
      <%= repair.date_completed %><br>
      <%= repair.description %><br></p>
      <%= link_to "Edit", edit_car_issue_repair_path(@car, @issue.id, repair.id) %><br>  <% end %>
</div>
  <head>
    <title>Place searches</title>
    <meta name="viewport" content="initial-scale=1.0, user-scalable=no">
    <meta charset="utf-8">
    <style>
      /* Always set the map height explicitly to define the size of the div
       * element that contains the map. */
      #map {
        height: 100%;
        width: 100%;
      }
      /* Optional: Makes the sample page fill the window. */
      html, body {
        height: 100%;
        margin: 0;
        padding:0;
      }
    </style>
    <script>
      // This example requires the Places library. Include the libraries=places
      // parameter when you first load the API. For example:
      // <script src="https://maps.googleapis.com/maps/api/js?key=YOUR_API_KEY&libraries=places">

      var map;
      var infowindow;

      function initMap() {

        navigator.geolocation.getCurrentPosition(function(position) {
            var pos = {
              lat: position.coords.latitude,
              lng: position.coords.longitude
            };

          // var pyrmont = {lat: 37.773972, lng: -122.431297};
          console.log(pos)
          map = new google.maps.Map(document.getElementById('map'), {
            center: pos,
            zoom: 15
          });

          
          console.log(pos)
          infowindow = new google.maps.InfoWindow();


          var service = new google.maps.places.PlacesService(map);
          service.nearbySearch({
            location: pos,
            radius: 2000,
            type: ['car_repair']
          }, callback);


    });

      }

      function callback(results, status) {
        console.log(results)
        if (status === google.maps.places.PlacesServiceStatus.OK) {
          for (var i = 0; i < results.length; i++) {
            createMarker(results[i]);

          }
        }
      }

      function createMarker(place) {
        var placeLoc = place.geometry.location;
        var marker = new google.maps.Marker({
          map: map,
          position: place.geometry.location
        });

        google.maps.event.addListener(marker, 'click', function() {
          var busName = place.name
          var busAddress = place.vicinity
          var busRating = place.rating
          var content = '<h3>' + busName + '</h3><br>' + busAddress + '<br>rating:' + busRating
          infowindow.setContent(content);
          infowindow.open(map, this);
        });
      }

      function handleLocationError(browserHasGeolocation, infoWindow, pos) {
            infoWindow.setPosition(pos);
            infoWindow.setContent(browserHasGeolocation ?
                                  'Error: The Geolocation service failed.' :
                                  'Error: Your browser doesn\'t support geolocation.');
      }
    </script>
  </head>
  <body>
    <div id="map"></div>
    <script src="https://maps.googleapis.com/maps/api/js?key=<%= ENV['GOOGLE'] %>&libraries=places&callback=initMap" async defer></script>
  </body>